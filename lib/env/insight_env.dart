// Microsoft Clarity project id.  Unique per project — never reuse
// another project's id.  Kept in its own file so the value can be
// swapped without touching the Insight facade or its call sites.
const String kClarityProjectId = 'xme4ws6ecb';
